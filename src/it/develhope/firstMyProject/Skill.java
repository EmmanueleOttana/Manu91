package it.develhope.firstMyProject;

public class Skill {
    int powerOfFire = (int) (Math.random()*500+500);
    int powerOfWater = (int) (Math.random()*500+500);
    int powerOfWind = (int) (Math.random()*500+500);
    int powerOfEarth = (int) (Math.random()*500+500);

}
